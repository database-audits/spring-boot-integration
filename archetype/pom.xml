<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.database-audits</groupId>
    <artifactId>database-audits-parent</artifactId>
    <version>1.2.1</version>
    <relativePath>../../parent/pom.xml</relativePath>
  </parent>

  <artifactId>database-audits-spring-boot-integration-archetype</artifactId>
  <version>1.3.0</version>
  <packaging>maven-archetype</packaging>

  <name>Database Audits Spring Boot Integration Archetype</name>
  <description>
    Maven archetype that scaffolds a ready-to-run database-audits example suite — the base IT class, the
    catalog/jpa/runtime audit ITs, and a Testcontainers PostgreSQL demo harness — into a consumer's project.
  </description>

  <developers>
    <developer>
      <id>jeffjensen</id>
      <name>Jeff Jensen</name>
      <email>jjensen@apache.org</email>
    </developer>
  </developers>

  <ciManagement>
    <system>GitHub Actions</system>
    <url>https://github.com/database-audits/spring-boot-integration/actions</url>
  </ciManagement>
  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/database-audits/spring-boot-integration/issues</url>
  </issueManagement>
  <scm>
    <connection>scm:git:https://github.com/database-audits/spring-boot-integration.git</connection>
    <developerConnection>scm:git:https://github.com/database-audits/spring-boot-integration.git</developerConnection>
    <url>https://github.com/database-audits/spring-boot-integration</url>
    <tag>v1.3.0</tag>
  </scm>

  <properties>
    <!-- This module has no src/site; its documentation lives in the integration module's site
         (archetype.adoc). Skip Maven site generation so a reactor-wide `mvn site` / `site:stage`
         does not build (or try to stage) an empty archetype site. The published site is the
         integration module's, generated on its own via `mvn site -pl integration`. -->
    <maven.site.skip>true</maven.site.skip>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- archetype:integration-test reads its test projects from target/test-classes/projects (the mojo's
         testProjectsDirectory default, ${project.build.testOutputDirectory}/projects). The maven-archetype
         lifecycle copies src/test/resources there via process-test-resources, so filtering archetype.properties
         expands databaseAuditsVersion=@project.version@ to the current build version before the IT mojo reads
         it — keeping the generated sample project depending on the freshly built integration artifact across
         release version bumps. The inherited Spring Boot resource config uses @...@ delimiters (not ${...}),
         so the property uses @project.version@. Only archetype.properties is filtered; the rest is copied verbatim. -->
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>**/archetype.properties</include>
        </includes>
      </testResource>
      <testResource>
        <directory>src/test/resources</directory>
        <excludes>
          <exclude>**/archetype.properties</exclude>
        </excludes>
      </testResource>
    </testResources>
    <extensions>
      <!-- Provides the maven-archetype packaging/lifecycle (package -> archetype:jar, integration-test -> generate + build the sample). -->
      <extension>
        <groupId>org.apache.maven.archetype</groupId>
        <artifactId>archetype-packaging</artifactId>
        <version>3.4.1</version>
      </extension>
    </extensions>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-archetype-plugin</artifactId>
          <version>3.4.1</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Fail early with a clear message if Docker is not running (required for Testcontainers). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>check-docker-running</id>
            <phase>validate</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <exec executable="docker" resultproperty="docker.exit.code" failonerror="false" outputproperty="docker.output" errorproperty="docker.error">
                  <arg value="info" />
                </exec>
                <fail message="Docker is not running. Please start Docker (Rancher Desktop) before running the build.">
                  <condition>
                    <not>
                      <equals arg1="${docker.exit.code}" arg2="0" />
                    </not>
                  </condition>
                </fail>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- The maven-archetype packaging lifecycle does not bind compile or test phases by default,
           so these executions are added explicitly to compile test sources and run unit tests. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>default-testCompile</id>
            <phase>test-compile</phase>
            <goals>
              <goal>testCompile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!-- Scope surefire to only our own test classes. The archetype:integration-test goal (bound to
           integration-test phase, which runs after test) compiles the generated sample project under
           target/test-classes/projects/, so surefire must not scan that subtree. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <executions>
          <execution>
            <id>default-test</id>
            <phase>test</phase>
            <goals>
              <goal>test</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <includes>
            <include>io/github/databaseaudits/**/*Test.java</include>
          </includes>
        </configuration>
      </plugin>
      <!-- The archetype module has no failsafe ITs of its own; its end-to-end verification is
           archetype:integration-test. Skip failsafe so it does not try to load the generated
           project's *IT.class files from target/test-classes/projects/. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
